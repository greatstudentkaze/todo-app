import React from 'react';

import TaskSearchForm from '../task-search-form';

const Header = () => {
  return (
    <header className="page-header">
      <TaskSearchForm />
    </header>
  );
};

export default Header;
